{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "NodeNext", // Ensures compatibility with ESM
    "moduleResolution": "NodeNext", // Resolves ESM imports
    "target": "ESNext",
    "outDir": "./dist",
    "rootDir": ".",
    "types": ["node"],
    "baseUrl": "."
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"],
  "references": [
    {
      "path": "../database"
    }
  ]
}
